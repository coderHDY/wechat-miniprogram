<van-cell title="{{types[currentIndex]}}" value="种类" is-link bind:click="showPopup" value-class="value-class" title-class="title-class" />

<goods-list showGoods="{{goodsList}}"/>

<van-popup show="{{ show }}" bind:close="onClose" show="{{ show }}" position="right" bind:close="onClose" custom-style="height:100%;">
	<van-sidebar active-key="{{ activeKey }}">
		<van-sidebar-item wx:for="{{types}}" title="{{item}}" bind:tap="changeType" data-index="{{index}}" wx:key="index"/>
	</van-sidebar>
</van-popup>